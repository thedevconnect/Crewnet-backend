# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=root@123
DB_NAME=crewnet
DB_PORT=3306

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production

# CORS Configuration
# Comma-separated allowlist of frontend origins (recommended for production)
# Examples:
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-app.vercel.app
CORS_ORIGINS=
#
# Optional: allow Vercel preview deployments via regex
# Example (adjust "your-app" to your Vercel project):
# CORS_ORIGIN_REGEX=^https:\/\/your-app-.*\.vercel\.app$
#
# If you use cookies/auth sessions, keep credentials enabled (default true)
# CORS_CREDENTIALS=true
#
# Escape hatch (NOT recommended): allow all origins only when credentials are off
# CORS_ALLOW_ALL=true

# Railway will automatically inject these environment variables when deployed
# No need to create .env file on Railway - just set variables in Railway dashboard
